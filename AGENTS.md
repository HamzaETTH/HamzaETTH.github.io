# Repository Workflow

- After implementing and verifying a requested feature or fix, commit and push the scoped changes unless the user explicitly says not to publish them.
- Preserve unrelated working-tree changes. Stage only the files or hunks that belong to the completed request.
